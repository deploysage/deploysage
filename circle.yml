---
machine:
  environment:
    RAILS_ENV: test
    RACK_ENV: test
  ruby:
    version: 2.2.3
  node:
    version: 5.3.0
test:
  override:
    - bin/start-spec:
        background: true
    - bin/rake ci
